<UserControl x:Class="ImageViewer.Views.ImageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:VM ="clr-namespace:ImageViewer.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext> 
        <VM:ImageViewModel></VM:ImageViewModel>
    </UserControl.DataContext>
    <Grid Width="200" Height="200">
        <Image Source="{Binding ImageSource}" Height="{Binding Rows}" Width="{Binding Columns}">
            
        </Image>
    </Grid>
</UserControl>
