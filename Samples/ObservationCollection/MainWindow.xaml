<Window x:Class="ObservationCollection.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ObservationCollection"
        xmlns:viewModel="clr-namespace:ObservationCollection.ViewModels"
        mc:Ignorable="d"
        Title="DataGrid" Height="Auto" Width="525">
    <Window.DataContext>
        <viewModel:OCViewModel></viewModel:OCViewModel>
    </Window.DataContext>
    <StackPanel>
        <TextBlock Text="Displaying grid of string" FontWeight="Bold" Margin="10,10"></TextBlock>
        <DataGrid ItemsSource="{Binding StringList}"></DataGrid>
        <TextBlock Text="Displaying grid of class with 2 properties X and Y"
                   FontWeight="Bold"
                   Margin="10,10"></TextBlock>
        <DataGrid ItemsSource="{Binding ClownList}" ColumnWidth="*"  Margin="10"/>
        <TextBox Text="{Binding Name}" Width="100" Margin="20,20" HorizontalAlignment="Left"></TextBox>
        <Button Content="Add" Margin="20,20" Width="Auto" Click="Button_Click" ></Button>
    </StackPanel>
</Window>
