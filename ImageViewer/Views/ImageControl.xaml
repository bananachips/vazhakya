<UserControl x:Class="ImageViewer.Views.ImageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageViewer.Views"
             xmlns:ViewModel="clr-namespace:ImageViewer.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <ViewModel:ImageViewModel></ViewModel:ImageViewModel>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition ></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Image x:Name="ImageX" Source="{Binding ImageSource}" MouseDown="Image_MouseDown"></Image>
        <TextBlock Text="{Binding Coord}" Grid.Column="1" VerticalAlignment="Center"></TextBlock>
    </Grid>
</UserControl>
